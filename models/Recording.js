class Recording {
    constructor(id, title, audioUri, date, duration, flags) {
        this.id = id;
        this.title = title;
        this.audioUri = audioUri;
        this.date = date;
        this.duration = duration;
        this.flags = flags;
    }
}

export default Recording;